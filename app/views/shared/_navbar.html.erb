<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
   <%= link_to owner_path(current_user)   do  %>
                      <span class="logo-button"><p>VetBook</p></span>
                    <% end %>
<!--   <%= link_to "VetBook", class: "navbar-brand"%>
 -->
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto justify-content-between">
      <% if user_signed_in? %>

        <div class="nav-item active d-flex justify-content-around mr-3">
          <%= link_to owner_path(current_user) do  %>
                      <span class="profile-button"><p>My Profile  </p></span>
                    <% end %>

         </div>

        <div class="nav-item  d-flex justify-content-around ">
            <%= link_to destroy_user_session_path, method: :delete do  %>
                      <span class="logout-button"><p>Log out</p></span>
                    <% end %>
        </div>

          <!-- <%= link_to "Log out", destroy_user_session_path, method: :delete,class: "nav-link" %> -->
        <div class="nav-item dropdown">
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Action", "#", class: "dropdown-item" %>
            <%= link_to "Another action", "#", class: "dropdown-item" %>
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
          </div>
      <% else %>
        <div class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
      <% end %>
    </ul>
  </div>
</div>


